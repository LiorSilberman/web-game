# Use a lightweight Node.js image
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Install deps first (better layer caching)
COPY package*.json ./
RUN npm install

# Copy the rest of your backend source code
COPY . .

# Expose backend port
EXPOSE 3001

# Run TypeScript directly with tsx in watch mode (defined in package.json)
CMD ["npm", "run", "dev"]
